# Sample check
check:
  local_rngd_proc:
    command: "PATH=$PATH:/usr/lib64/nagios/plugins:/usr/lib/nagios/plugins check_procs -C rngd -u rngd -c 1:1"
    interval: 60
    occurrences: 1
    subscribers:
    - local-rngd-server
